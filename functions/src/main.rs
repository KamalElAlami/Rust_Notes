// simple function

fn simple_function()
{
    println!("i m just a simple function 💅🎀");
}
// function with return value

fn addition(a:i32, b:i32) -> i32
{
    return (a + b);
}

// function can return value without using return keyword

fn division(a:i32, b:i32) -> i32
{
    a / b
}

fn main() {
    println!("Hello, world!");
}
